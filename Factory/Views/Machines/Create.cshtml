@{
  Layout = "_Layout";
}

@model Factory.Models.Machine

<h3>Add a new machine</h3>
<hr>

@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.MachineName)
  @Html.TextBoxFor(model => model.MachineName, new {@class = "fomr-control", @required = "required"})
  <input type = "submit" value = "Add new machine" class="btn btn-dark"/>
}

<p class="btn btn-outline-dark">@Html.ActionLink("Show all machines", "Index")</p>
<p class="btn btn-outline-dark">@Html.ActionLink("Home", "Index", "Home")</p>